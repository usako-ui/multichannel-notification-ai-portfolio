# project-overview.md
# マルチチャネル通知システム｜プロジェクト概要

---

## プロジェクト名

不動産管理会社向け マルチチャネル通知システム（案件5）v1.0

---

## 背景・現状課題

| 課題 | 現状 | 影響 |
|---|---|---|
| 問い合わせチャネルの分散 | Gmail・LINE・電話の3チャネルに分散。担当者が個別に受信トレイを確認する運用 | 確認漏れ・対応遅延が発生しやすい |
| LINEの営業時間外見落とし | ヒアリングで最多の取りこぼしチャネルと確認済み | 成約機会の損失・顧客満足度の低下 |
| クレーム対応の遅延 | 問い合わせを手動で確認するため認知が遅れる | 対応遅延による退去・評判悪化・最悪訴訟 |
| 問い合わせ分類の属人化 | 担当者が手動でカテゴリを判断している | 経験・判断基準によってばらつきが生じる |

---

## 目的・解決策

Gmail・LINE公式に届いた問い合わせを Slack へ自動集約し、
Gemini API で 5 カテゴリに自動分類して担当チャネルへ振り分ける。
クレームは即時検出して営業部長の個人 LINE へ通知することで、
**対応遅延ゼロ・取りこぼしゼロ** を実現する。

まず MVP で Gmail・LINE の集約と自動分類を実現し、
実際の運用データをもとに段階的に電話・管理画面へ拡張する。

---

## 対象ユーザー

| ユーザー種別 | 人数 | 目的・使い方 |
|---|---|---|
| 管理者（営業部長） | 1名 | クレームの緊急通知を個人 LINE で受信。システム監視担当を兼任 |
| 現場スタッフ | 3〜5名 | Slack の各カテゴリチャネルを確認して問い合わせに対応する |

> **【要確認】** 現場スタッフの正確な人数はヒアリング回答③待ち。3〜5名で仮設計。

---

## KPI・成功指標

> 現時点で見落とし件数・対応時間の計測値はない。
> 導入初月から計測を開始してベースラインを設定し、3ヶ月後の数値を成果指標とする。

| 指標 | 現状 | 目標値 | 根拠 |
|---|---|---|---|
| LINEの見落とし件数 | 未計測（最多と確認済み） | 導入3ヶ月後にゼロ | Webhook 即時転送で営業時間外も即通知 |
| クレーム認知時間 | 未計測 | 5分以内（SLA目標） | 緊急パスによる即時処理（キュー非経由） |
| 担当者の確認チャネル数 | 3チャネル | 1チャネル（Slackのみ） | 全チャネルを Slack に自動集約 |

---

## ユーザーストーリー（優先度 High）

- 現場スタッフとして、Slack だけ見れば全チャネルの問い合わせが確認できる。
  複数の受信トレイを確認する手間がなくなる。
- 営業部長として、クレームを 5 分以内に個人 LINE で受け取れる。
  対応遅延による顧客満足度の低下を防げる。
- 現場スタッフとして、AIが問い合わせをカテゴリに振り分けてくれる。
  自分が担当するカテゴリのチャネルだけ見ればよい状態になる。

---

## 問い合わせカテゴリ比率（ヒアリング確認済み）

| カテゴリ | 比率 | 月間件数（750件ベース） | MVPでの対応 |
|---|---|---|---|
| 賃貸問い合わせ | 50% | 約375件 | Slack `#賃貸` へ自動振り分け |
| 売買問い合わせ | 20% | 約150件 | Slack `#売買` へ自動振り分け |
| 内見希望 | 20% | 約150件 | Slack `#内見` へ自動振り分け |
| クレーム | 10% | 約75件 | 緊急パス → 即時 LINE Push（SLA 5分以内） |
| 要確認・その他 | - | 随時 | Slack `#要確認` へ振り分け。担当者が目視確認 |

> **カテゴリ定義の重要ルール：**
> 「クレームかどうか迷う」場合は必ず「クレーム」に分類する（安全側に倒す）。
> 「カテゴリが判断できないもの・迷惑メール等」は「要確認・その他」に分類する。

---

## システム全体のデータフロー

```
Gmail / LINE公式
  ↓（Webhook）
Vercel Functions（app/api/webhooks/）
  ↓（緊急キーワード判定）
  ├── 緊急（クレーム系キーワード検出）
  │     → handleUrgent()
  │       ① Supabase に保存（is_urgent=TRUE・status=classified）
  │       ② Slack `#クレーム緊急` に投稿（記録用）
  │       ③ 営業部長 個人LINE へ Push（SLA 5分以内）
  └── 通常
        → Supabase inquiry_queue へ保存（status=pending）
              ↓（GitHub Actions Cron 5分ごと + main push トリガー）
        app/api/cron/classify/route.ts
              ↓（最大20件/回）
        classifyWithGemini()
        → 5カテゴリに分類
              ↓
        postToSlack()
        → 各カテゴリの Slack チャネルへ投稿（#賃貸 / #売買 / #内見 / #要確認）
              ↓
        inquiry_queue status → notified
```

---

## 技術選定の背景

| 技術 | 選定理由 |
|---|---|
| Next.js 14 | Vercel との親和性・App Router で Webhook / Cron を同一プロジェクトで管理できる |
| Supabase | DB・キュー管理をワンストップ提供。無料プランで MVP 検証が可能 |
| Gemini API | 検証環境のコスト最小化（無料枠）。本番移行時に再評価する |
| GitHub Actions Cron | Vercel Hobby プランの Cron 1 日 1 回制約を回避しつつ 5 分間隔を無料枠で実現。`main` push トリガーを併設して schedule 遅延をカバー |
| Vercel Functions | Webhook 受信器として機能。常時起動サーバー不要でコストを削減 |

> **Gemini API は今回の検証環境のみ。**
> 実運用開始時はデータ送信ポリシー・コスト・精度を再評価して最終構成を決定する。

---

## 制約・前提

- 開発期間：要件定義確定後 約7営業日（テスト・修正バッファ込み）
- 予算：初期 148,000 円 / 月額 約 3,000〜3,300 円（実運用時）
- Vercel：Hobby プラン（Cron は 1 日 1 回まで。**GitHub Actions Cron に外部化して回避**）
- Supabase：無料プラン（1 週間非活動でプロジェクトが一時停止する。週 1 回以上操作すること）
- LINE 公式：無料プラン（Push Message は月 200 通まで。クレームは月 75 件想定で範囲内）
- Gemini API は検証環境のみ。本番移行時に有料 API へ切り替えて再検証する

---

## Phase 2 以降の拡張計画

| 機能 | 目的 | 追加費用目安 |
|---|---|---|
| 電話の自動化（Twilio連携・録音→テキスト変換） | 電話 50 件/月も Slack に集約する | +約 12 万円（初期）+約 500 円/月 |
| 対応状況管理画面（未対応/対応中/完了） | 問い合わせの対応状況を可視化・再通知 | 別途見積 |
| 過去問い合わせの全文検索 | 類似問い合わせの参照・ナレッジ蓄積 | 別途見積 |
| SLA レポートの自動生成 | 月次の対応品質レポートを自動作成 | 別途見積 |
| 営業担当の自動アサイン | カテゴリ・担当者ルールに基づいて自動割り当て | 別途見積 |

---

## 【要確認】提案書フィードバック後に確定する事項

| 項目 | 現在の仮設計 | 確認方法 |
|---|---|---|
| Slack チャネルの正式名称 | `#賃貸`・`#売買`・`#内見`・`#クレーム緊急`・`#要確認` で設計済み | 変更が必要な場合はヒアリング回答後に確定 |
| クレーム境界線の具体例 | No.19・No.20（テスト CSV）で設計 | ヒアリング⑩回答待ち |
| 現場スタッフの正確な人数 | 3〜5 名で仮設計 | ヒアリング回答③確認後に確定 |
| Gmail 署名検証の設計 | 実装時に方式を決定 | Webhook 実装フェーズ着手前に確認 |

---

## 参考資料

| 資料 | 内容 |
|---|---|
| `case5-test-inquiries.csv` | テスト問い合わせ 22 件（分類精度確認・境界線テスト用） |
| `docs/manual-operator.md` | 運用マニュアル（クライアント向け・実装完了後に作成） |
| `docs/manual-developer.md` | 開発者向け手順書（APIキーローテーション等・実装完了後に作成） |
| `AGENTS.md` | AI 引き継ぎ用ドキュメント（納品物・実装完了後に「触ると壊れる箇所」を追記） |
